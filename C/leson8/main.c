#include <stdio.h>
#include "func.h"

// указатель - это адресс другого объекта

// * - это оператор обращения 

int main(){
   

    /* int num = 0;
    int * pNum = &num;
    printf("%p\n", pNum);

    *pNum = 10;
    printf("%d\n", *pNum);
 */
    func();


    return 0;
}